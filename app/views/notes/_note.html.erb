<%= link_to note_path(note), action: 'edit' do %>
<div class="col-sm-4 col-md-3 note-box">
    <%# tag.img src: (note.img_url? ? "#{asset_path(note.img_url)}" : image_url("default.png")), class: 'note-image' %>
    <div class='note-topic'>
	    <h3><%= note.topic %></h3>
	</div>
	<div class='note-text'>
		    <%= note.description %>
	</div>
</div>
<% end %>